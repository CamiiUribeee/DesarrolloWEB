---
import Layout from '../layouts/Layout.astro';

---

<Layout title="Welcome to Astro.">
	hola mundo
	<div class="text-red-500">cruel</div>


	<div id="btn" class="bg-pink-400 p-3 inline-block">
		presioname
	</div>
	<div class="embla">
		<div class="embla__container p-12 h-52">
		  <div class="embla__slide bg-purple-300 ">
			Slide 1
		  </div>

		  <div class="embla__slide bg-blue-200">
			Slide 2
		  </div>

		  <div class="embla__slide bg-yellow-100">
			Slide 3
		  </div>
		</div>
	</div>
	  
</Layout>



<script>
	import JSConfetti from 'js-confetti'
	import EmblaCarousel from 'embla-carousel'
	const emblaNode = document.querySelector('.embla')
	const options = { loop: false }
	EmblaCarousel(emblaNode, options)


	const btn = document.getElementById("btn")

	btn.addEventListener("click", function (e){
		const elm = e.target

		//elm.classlist.remove("bg-red-400")
		//elm.classList.add("bg-blue-400")
		elm.classList.replace("bg-pink-400", "bg-blue-500")

		elm.innerText = "Ouch"

		const myDiv = document.createElement("div")
		myDiv.innerText = "soy un div creado con js"
		myDiv.classList.add("bg-green-500", "text-white")

		myDiv.addEventListener("click", (e) => {
			e.target.remove()
			console.log("Harakiri", e.target)
		})

		document.body.appendChild(myDiv)

		const jsConfetti = new JSConfetti()
		jsConfetti.addConfetti()
	
		jsConfetti.addConfetti({
   			emojis: ['♞', '✭', '🎼', '♬', '✞', '☠'],
		})

		


	})

	
	
																					


</script>
<style>
.embla {
  overflow: hidden;
}
.embla__container {
  display: flex;
}
.embla__slide {
  flex: 0 0 100%;
  min-width: 0;
}

</style>

